# BOT

This directory contains bot, it is possible that is completely broken and you have to rewrite some stuff


# Installation

`pip3 install -t requirements.txt`

`touch .env`

`echo -e "TOKEN=YOUR_BOT_TOKEN" >> .env`
